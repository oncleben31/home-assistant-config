---
# E: Manage (Install, track, upgrade) custom elements for Home Assistant directly from the UI
# Can't put it here. Currently only compatible if put in configuration.yaml
# F: Gestion de composants tiers directement depuis l'interface.
# Cela ne donctionne pas avec ce fichier. Pour le moment il faut le mettre directement dans
# configuration.yaml
#
# https://github.com/custom-components/hacs
#
hacs:
  token: !secret my_github_access_token
